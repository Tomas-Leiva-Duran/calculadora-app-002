import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'calculadora.app.v002',
  appName: 'calculadora-app-002',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
